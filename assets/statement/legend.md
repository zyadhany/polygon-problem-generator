You are given an array $a$ of $n$ integers and an integer $x$.

Find two distinct indices $i$ and $j$ such that:
$$
a_i + a_j = x
$$

If such indices exist, output them.  
Otherwise, output \texttt{IMPOSSIBLE}.

Indices are numbered from $1$ to $n$.